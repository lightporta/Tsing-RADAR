# Tsing-RADAR Backend Dependencies
# Python 3.10+

# —— Web 框架 ——
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# —— 配置与校验 ——
pydantic==2.9.2
pydantic-settings==2.5.2

# —— 数据库 ——
SQLAlchemy==2.0.35
alembic==1.13.3
# PostgreSQL 生产期启用（开发期用 SQLite 内置）：
# psycopg2-binary==2.9.9

# —— 缓存（可选，未安装降级为内存）——
# redis==5.0.8

# —— 向量数据库（可选，未安装降级为 hash 向量）——
# pymilvus==2.4.7

# —— HTTP 客户端（调用 LLM API）——
httpx==0.27.2

# —— 对话编排（可选，未安装降级为函数链）——
# langgraph==0.2.34
# langchain-core==0.3.1

# —— 测试 ——
pytest==8.3.3
pytest-asyncio==0.24.0
