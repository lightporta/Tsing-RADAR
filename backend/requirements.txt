# Tsing-RADAR Backend Dependencies
# Python 3.10+

# —— Web 框架 ——
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9  # /api/storage/upload 的 UploadFile 需要

# —— 配置与校验 ——
pydantic==2.9.2
pydantic-settings==2.5.2

# —— 数据库 ——
# 统一使用 MySQL 8.x（本地开发与生产同方言）
# 本地启动 MySQL 8.x：
#   Docker:  docker run -d --name tsing-mysql -p 3306:3306 \
#              -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=teacher_db \
#              mysql:8.0 --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
#   Mac:     brew install mysql@8 && brew services start mysql@8
# 数据库本身需手动 CREATE DATABASE teacher_db（不要建表，交给 alembic）
SQLAlchemy==2.0.35
alembic==1.13.3
PyMySQL==1.2.0
cryptography>=43.0.1  # PyMySQL caching_sha2_password 鉴权所需

# —— 缓存（可选，未安装降级为内存）——
# redis==5.0.8

# —— 向量数据库（可选，未安装降级为 hash 向量）——
# pymilvus==2.4.7

# —— HTTP 客户端（调用 LLM API）——
httpx==0.27.2

# —— 对话编排（可选，未安装降级为函数链）——
# langgraph==0.2.34
# langchain-core==0.3.1

# —— 测试 ——
pytest==8.3.3  # unused
pytest-asyncio==0.24.0  # unused
