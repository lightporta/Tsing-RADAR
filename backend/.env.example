# Tsing-RADAR 后端本地开发环境变量示例
# 复制为 .env 使用：cp .env.example .env
# 生产部署不使用本文件（只使用 LLM_PROVIDER + LLM_API_KEY_FILE，
# 并强制覆盖全部签名密钥，见 deploy/production/production.env.example）。

# —— LLM（智谱 GLM，可选）——
# 本地开发直接填 key；留空则表达层自动降级为固定模板（功能不受影响）。
# GLM_API_KEY=
# GLM_CHAT_MODEL=glm-4-flash
# GLM_EMBED_MODEL=embedding-3
# GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# —— 会话签名（仅本地开发默认值，生产必须覆盖）——
# SESSION_HMAC_SECRET=local-development-only-change-me

# —— 清小搭文本雷达图形态（可选）——
# auto（默认，退化数据自动降级柱状图）/ radar / bars
# RADAR_TEXT_FORM=auto
