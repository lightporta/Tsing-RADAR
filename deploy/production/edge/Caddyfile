{
    admin off
    servers {
        timeouts {
            read_body 20s
            read_header 10s
            write 120s
            idle 60s
        }
        max_header_size 32KB
    }
}

import /etc/caddy/routes/*.caddy

:80, :443 {
    header -Server
    respond "Host not allowed" 421
}
